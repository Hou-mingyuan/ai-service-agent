# AI Service Agent 最终发现与决策

## 当前结论

- 项目已从演示壳收敛为可运行的客服 Agent v1 发布候选版；统一标准 7 个维度和专项 10 项能力均已有实现与证据。
- 最新后端回归 40/40；前端 lint/typecheck/Vitest 8/8/build；固定对话评估 38/38。
- 最新真实浏览器闭环为会话 #7、工单 `TK20260720-33A16047`，完整覆盖订单工具、负面转人工、队列、认领、双向消息、解决、5 星评价和关闭；两端控制台 0 错误/警告。
- 当前无缓存 Compose 镜像自动验收为会话 #13、工单 `TK20260720-B7B6EDFC`；原生 H2/Vite smoke 为会话 #3、工单 `TK20260720-8FA47FB4`，均完整 PASS。
- 当前 MySQL 为 15 表、4 用户、5 知识文档、8 工单、13 会话、5 评价、6 工具执行单，flush level 为 1。
- 默认 MySQL 持久化配置下，普通读 p95 250.57 ms、核心写 p95 554.01 ms、SSE p95 2.08 s，业务错误率均为 0。

## 冻结技术决策

| 决策 | 原因 |
| --- | --- |
| 保持 Spring Boot + Vue 单体、H2/MySQL、MyBatis-Plus | 改动面最小，支持本地与 Compose，无需为当前规模引入分布式复杂度 |
| 内部自增主键，对外使用随机会话/工单标识 | 保留单库效率并降低 ID 枚举风险 |
| 应用、JSON 与 MySQL 时区统一为 `Asia/Shanghai` | 与运行地和运营日期口径一致，避免页面/看板日期偏移 |
| Mock/HTTP 业务 adapter 与 Mock/OpenAI LLM 分别共享接口 | 同一工具/页面契约，来源可见，真实配置失败不静默降级 |
| 敏感工具先建待确认执行单，再由确认 API 原子执行 | 将确认、权限、有效期、幂等与审计做成可独立验证的边界 |
| WebSocket 只推可重放事件，写入使用幂等 REST | 业务语义清晰；持久事件 + `afterId` 支持断线恢复与受众隔离 |
| SLA 使用数据库条件更新和唯一事件键 | 多实例由数据库仲裁，不依赖进程内锁，不会重复升级 |
| 前端采用安静、密集、可扫描的客服控制台 | 队列、SLA、时间线和状态优先，不使用营销式渐变/装饰卡片 |

## 已验证事实

- 四角色账号持久化，BCrypt cost 12；Bearer 和 HttpOnly Cookie 均可认证，Cookie 写操作强制 CSRF。
- 会话、消息、工单、工具执行单、实时事件和知识文档同时校验租户、角色和资源归属；越权枚举返回 404。
- 知识检索支持文档、分块、来源、租户、归档和无答案；`KB-1-1` 来源在 API 与页面均可见。
- 六个工具均有 JSON Schema、参数/权限/归属、超时、有限重试、幂等、审计和来源。
- 改期确认前无副作用，首次确认单次执行，重复确认重放；请求和执行审计齐全。
- 人工队列原子认领，机器人暂停；客户/坐席消息双向实时、幂等、已读；评价后会话与工单一致关闭。
- 工单创建/认领/SLA 并发唯一性由集成测试证明；工单支持合法流转、指派、解决、关闭原因和重开。
- 看板指标按时间范围从真实业务表计算，不使用硬编码展示数字。
- 全新 MySQL 初始化 15 表，seed 重启不重复；匿名 health/readiness、Nginx 配置/缓存/安全头正常。

## 外部边界

- 当前没有真实 LLM key 或真实业务系统凭据；已完成真实 adapter 合同、鉴权、超时/重试、私网限制和缺配置启动失败，不能据此声称外部质量/延迟通过。
- 当前发布候选版验证全新 v1 schema；已有旧库升级需要按实际旧 schema 另行审核迁移。
- 公网 TLS、边缘限流和集中密钥管理由部署平台提供；应用生产门禁会拒绝 Mock、默认密钥、匿名对话、通配 CORS 和非 MySQL 配置。

## 工具链事件

- Docker 内 Maven Central 曾出现一次 TLS 下载失败，使用可配置的可信 Maven 镜像后构建成功；没有把镜像写死进源码。
- Docker Desktop 在并发构建时曾退出一次，恢复后拆分构建并完成全栈复验，不影响交付产物。
- 内置浏览器连接运行时连续两次初始化冲突；按浏览器技能回退到 Playwright CLI 有界面 Chromium，完整 E2E 与截图成功。
- 宿主环境继承了 `NODE_TLS_REJECT_UNAUTHORIZED=0`，Playwright CLI 启动器打印警告；应用浏览器控制台为 0/0，npm 高危审计已显式在 TLS 校验开启时通过。
- 最终证据采集时，3 条 Windows 只读命令因 PowerShell/cmd 引号与 `find` 参数解析失败；改用 PowerShell 原生对象查询与分离参数后成功，未修改文件或数据。
- 封账前 Playwright 再次打开客户会话 #7 和坐席工作台：关闭态动作全部禁用，两端控制台继续为 0 errors / 0 warnings；临时浏览器目录已删除。
